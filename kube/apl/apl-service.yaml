# Create a service
kind: Service
apiVersion: v1
metadata:
  name: apl-fila
  annotations:
    eks.amazonaws.com/role-arn: "arn:aws:iam::813173347981:role/eks-eks-nodes"
spec:
  selector:
    app: apl-fila
  ports:
    - name: apl-fila
      targetPort: 9090
      port: 9090
  type: LoadBalancer
